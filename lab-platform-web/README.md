# 芯栈 Lab · 嵌入式实验室管理系统

这是可运行的 HTML 前端演示，所有项目文件均位于 `/Users/du/Documents/project/guanlixitong`。无需安装依赖，未连接阿里云后端。开发与测试使用 Node.js 20+，本地预览使用 Python 3。

## 打开页面

- 正在运行的本地预览：http://127.0.0.1:4173
- 或者直接用浏览器打开本目录下 `index.html`，页面直接引用 `src/` 内的样式和脚本，无 CDN、网络字体或外部图片依赖。复制或部署时需同时保留 `index.html` 和 `src/` 文件夹。
- 推荐固定使用上述 HTTP 地址测试。直接打开文件、换端口或将 127.0.0.1 改为 localhost，都属于不同存储环境，演示数据不会自动共享。

如果预览服务停止，在终端执行：

```sh
cd /Users/du/Documents/project/guanlixitong
python3 -m http.server 4173 --bind 127.0.0.1
```

也可双击 `启动预览.command`。关闭启动终端或按 Ctrl+C 可停止它。

## 测试账号

以下 8 个初始测试账号的密码统一为 **Lab@123456**（大小写敏感）。新建成员使用管理员创建时设置的独立密码。

| 角色 | 姓名 | 账号 | 可测试的功能 |
| --- | --- | --- | --- |
| 指导老师 | 陈明远 | teacher | 全局查看、成员角色管理、模块管理、审批、发放与验收 |
| 负责人 | 林知远 | manager | 普通成员资料管理、模块管理、审批普通成员的申请 |
| 普通成员 | 张子涵 | member | 更新本人状态、请假、借用、查看自己的记录、申请归还 |
| 普通成员 | 李沐阳 | limuyang | 测试另一位成员的借用记录隔离 |
| 普通成员 | 王雨桐 | wangyutong | 正处于已批准的请假时段 |
| 普通成员 | 赵一鸣 | zhaoyiming | 有一笔逾期借用 |
| 普通成员 | 陈思远 | chensiyuan | 有一条待审批请假 |
| 普通成员 | 周可欣 | zhoukexin | 模拟离线，有一笔待确认归还 |

登录页的身份按钮只负责填入账号，点击“登录工作台”进入系统。在个人中心也可切换上述身份，不需要反复输入密码。新建成员必须设置独立初始密码（8–64 位，至少含一个英文字母和一个数字），并再次输入确认。

## 已实现

- 工作台：实时计算人员、资产、库存、逾期、待办和动态。
- 成员管理：新增成员并设置独立账号密码、编辑、角色设置、详情、停用/启用、搜索筛选与分页。老师和负责人可以创建账号，负责人仅能创建普通成员。
- 成员页增加状态快捷筛选和管理人员可见的账号列；新增表单分为成员资料和登录账号，创建成功后展示账号信息。
- 中文搜索支持输入法合成，手机侧栏支持 Escape 关闭与隐藏时禁止键盘聚焦。
- 工作状态：空闲、忙碌、模拟离线；请假状态由有效审批记录自动覆盖，到期恢复。
- 请假管理：提交、审批、拒绝、撤销；原因和意见按角色隐藏。
- 模块管理：18 件实物、13 种型号、6 类模块；一物一码，型号库存汇总，详情与历史。
- 模块借用：多件资产申请 → 审批 → 确认发放 → 归还申请 → 实物验收。
- 发放前检查资产状态，禁止重复发放；损坏资产归还后进入维修。
- 维修登记、维修完成、报废；报废和停用保留历史记录。
- 比赛管理：报名和比赛时间、参赛须知、负责人、官方链接、自定义流程节点、归档/恢复；工作台展示近期比赛。
- 本地持久化、同源标签页更新通知、恢复演示数据。
- 响应式侧栏、弹窗、状态提示、表单校验、键盘支持。

## 创建新成员账号

1. 使用 `teacher` 或 `manager` 登录。
2. 进入“成员管理”，点击“新增成员”。
3. 填写姓名、学号 / 工号、所属小组、登录账号。
4. 设置初始密码并再次确认，点击“创建成员账号”。
5. 成功提示中确认登录账号，然后退出登录，用新账号和刚设置的密码登录。

账号须为 3–30 位字母、数字或下划线，账号（不区分大小写查重）与学号均不得重复。登录时账号区分大小写。密码不会在成员列表、详情或操作记录中展示；演示数据只保存随机盐和 PBKDF2 校验结果，不保存自定义密码明文。新建账号的实际身份认证上线后仍必须放在后端执行。

已有成员的资料编辑不会改变其密码。原有演示数据及 8 个测试账号兼容保留。负责人只能创建和维护普通成员账号，指导老师可设置其他角色。

## 建议测试流程

1. 使用 `member` 登录，打开模块管理，选择空闲模块并申请借用。
2. 个人中心切换至 `manager`，在“借用与归还”找到申请并通过；此时模块仍为空闲。
3. 点击该记录的“确认发放”，确认实物已交付，模块转为使用中。
4. 切回 `member`，打开自己的借用记录，点击“申请归还”。
5. 切回 `manager`，点击“验收归还”。实物完好无需勾选；勾选损坏资产并填写问题可测试自动送修。
6. 查看模块管理、工作台与历史记录是否同步更新；刷新页面确认数据保留。
7. 使用 `chensiyuan` 提交或查看请假，切换负责人审批。开始时间可设为当前时间之前、结束时间设为未来，测试即时请假状态。
8. 普通成员不应看到新增成员、录入模块、审批按钮；负责人不能修改老师或负责人的角色，也不能审批自己的申请。

9. 以老师或负责人身份打开“比赛管理”，新增比赛并填写参赛须知，使用“添加节点”维护报名、初赛、决赛与答辩流程。切换普通成员验证只能查看，刷新后检查比赛仍保留。

个人中心的“恢复演示数据”会要求确认，覆盖当前浏览器中的演示修改并退出登录。浏览器测试可能已经改变个别初始记录，可用此功能恢复。

## 项目文件

```text
index.html                 页面入口，直接引用本地样式和脚本
src/styles.css             全部响应式样式
src/core.js                配置、工具、API、快照同步与公共业务规则
src/seed.js                初始演示数据（包含比赛）
src/ui.js                  表格、表单、弹窗、搜索与手机导航
src/app.js                 工作台、登录、导航与页面初始化
src/features.js            成员账号、模块、借用、请假页面及操作
src/competitions.js        比赛管理、流程节点及参赛须知
package.json               测试、校验和启动命令，无依赖包
启动预览.command           macOS 本地预览启动脚本
API.md                     阿里云后端对接契约
TEST-REPORT.md             验证记录与已知范围
测试账号.txt                可直接查看的账号清单
tests/core.test.mjs         核心业务规则测试
```

直接维护 `index.html` 和 `src/`，不再生成重复的内嵌脚本副本，也不需要打包。四类管理列表共用表格组件，按钮、表单字段和确认操作共用实现。

```sh
npm run check   # 运行全部自动化测试
npm start       # 启动 4173 端口预览（端口需空闲）
```

无需 `npm install`。也可直接运行 `node --test tests/core.test.mjs`。修改源码后刷新页面即可。

数据加载、跨标签页同步和失败回滚共用快照更新逻辑。同步不会关闭正在填写的表单；保存失败后可以在原表单重试。浏览器原有演示数据及自定义账号保持兼容。

## 阿里云对接

`src/core.js` 顶部的 CONFIG 预留 `mode` 与 `API_BASE_URL`。当前 `mode: 'mock'` 为本地演示；后端实现 `API.md` 中的接口后可改为 `api` 并刷新页面。

当前账号、数据、角色切换与权限隐藏均是前端演示，不构成生产认证或数据隔离。localStorage 不是阿里云数据库，真实账号密码不能使用这里的演示方案。上线时应由后端执行身份认证、权限检查、隐私字段裁剪和数据库事务。本次没有部署网站，也没有访问或修改阿里云服务器。
