# 实验室管理平台 — 界面可视化与排版分析报告 + 企业级优化方案

- **分析对象**：`http://localhost:8001/` 全部 19 个界面（列表 9 / 详情 5 / 仪表板 1 / 专用页 4）
- **方法**：Playwright 驱动真实浏览器，在 1440×900 与 390×844 两档视口逐页量化（滚动屏数、卡片与分区数、主内容起点、表格行数与列数、分页能力、横向溢出）；再用模板/视图源码定位结构性原因
- **原始数据**：`reports/qa/ui_metrics.json`、`ui_metrics2.json`；截图 `reports/qa/ui-*.png`
- **一句话结论**：**主题视觉已经成型（318 个设计令牌、统一像素风），但"信息架构 + 数据可见性 + 组件规范"这三层还是原型水平**——最典型的证据是：浏览记录页 **6.1 屏、104 行、0 个筛选器**，成员详情 **12 张卡片 / 10 个标题 / 移动端 3.2 屏**，打卡与浏览记录**根本没有分页**（硬编码 `[:200]` / `[:300]`）。

---

## 一、全站量化数据

### 1.1 桌面 1440×900

| 页面 | 滚动屏数 | 卡片数 | 卡片总高 | 表格行 | 主内容起点 | 分区标题 | 分页 |
|---|---|---|---|---|---|---|---|
| 仪表板 | 1.0 | **14** | 1978px | 10 | 687px (0.76 屏) | 5 | — |
| 硬件列表 | 1.0 | 2 | 317px | 3 | 230px | 2 | ✅ 有页大小控件 |
| 硬件详情 | 1.1 | 5 | 1171px | 15 | 225px | 6 | — |
| 任务列表 | 1.2 | 2 | 790px | 12 | 230px | 2 | ✅ |
| 任务详情 | 1.5 | 6 | 1794px | 7 | 241px | 6 | — |
| 我的任务 | 1.0 | 0 | 0 | 2 | 0 | 2 | 3 个标签页 |
| 借出列表 | 1.0 | 2 | 152px | 1 | 230px | 2 | ✅ |
| 项目列表 | 1.0 | 2 | 213px | 1 | 230px | 2 | ✅ |
| 工具管理 | 1.0 | 2 | 580px | 8 | 230px | 2 | ✅ |
| **打卡记录** | 1.0 | 1 | 174px | 3 | 230px | 2 | ❌ **无分页**、无筛选、无工具栏 |
| 打卡详情 | 1.0 | 2 | 1039px | 7 | 283px | 4 | — |
| **浏览记录** | **5.7** | 8 | **5452px** | **99** | 460px | 5 | ❌ **无分页**、无筛选 |
| 成员列表 | 1.0 | 2 | 467px | 0（卡片式） | — | 2 | ❌ 无分页 |
| **成员详情** | **2.1** | **12** | 2744px | 20 | 675px | **10** | — |
| 任务日历 | 1.1 | 1 | 129px | 6 | 229px | 3 | — |
| 通知中心 | 1.0 | 0 | 0 | 0 | — | 2 | ❌ `[:50]` 截断 |
| 数据导出 | 1.0 | 3 | 503px | 0 | 264px | 5 | — |
| 智能体控制台 | 1.0 | 0 | 0 | 0 | — | 1 | 会话 `[:20]`、消息 `[:200]` |
| 签到打卡 | 1.0 | 2 | 834px | 0 | 229px | 4 | — |

### 1.2 移动 390×844

| 页面 | 滚动屏数 | 主内容起点 | 备注 |
|---|---|---|---|
| **仪表板** | 2.0 | **1204px（1.4 屏以下）** | 10 张 KPI 卡纵向堆叠，把待办推到第二屏 |
| **浏览记录** | **6.1** | 608px | 104 行一次渲染 |
| **成员详情** | 3.2 | 861px（刚好在首屏外） | 12 张卡片 |
| 任务列表 | 1.9 | 370px | 表格 7 列 |
| 任务详情 | 1.7 | 273px | |
| 打卡详情 | 1.5 | 887px | |
| 工具管理 | 1.5 | 370px | |
| 其余 | ≤1.5 | 256–370px | 无横向溢出（这点做得好） |

### 1.3 分页能力实测（用 `?per_page=1` 强制多页）

| 列表 | `?per_page=1` 渲染行数 | 页大小控件 | 结论 |
|---|---|---|---|
| 任务 | 1（分页器显示 `1 2 3 … 7`） | 1/25/50/100/250/500 | ✅ 真分页 |
| 硬件 / 项目 / 工具 | 1 | 同上 | ✅ 真分页 |
| **打卡记录** | **3**（全部） | 无 | ❌ 硬编码 `[:200]` |
| **浏览记录** | **104**（全部） | 无 | ❌ 硬编码 `[:300]` |
| **通知** | — | 无 | ❌ 硬编码 `[:50]` |
| **成员** | — | 无 | ❌ Python 列表全量渲染 |

### 1.4 结构与样式盘点

| 指标 | 数值 | 判断 |
|---|---|---|
| 设计令牌 `--tp-*` | **318 个** | ✅ 基础很好，可直接升级为三层令牌 |
| `terminal_pixel.css` | 2982 行 / 118.6 KB | ⚠️ 偏大 |
| `lab_manager.css` | 720 行 / 30.3 KB | ⚠️ 与主题层重复覆盖 |
| `!important` 总数 | **449** | ❌ 说明样式在"对抗"Bootstrap，而非定义组件 |
| 模板数量 / 总行数 | 32 个 / 3228 行 | ⚠️ 平均 100 行，逻辑与视图混杂 |
| 内联 `<script>` 的模板 | **8 个** | ❌ 行为散落，难复用难测试 |
| 内联 `<style>` 的模板 | 1 个（`member_detail.html`） | ⚠️ 与主题表冲突 |
| 列表页表头数（含勾选/操作列） | 硬件 **10** / 工具 9 / 借出 8 / 项目 7 / 任务 7 | ⚠️ 宽表，窄屏需横向滚动 |
| 打卡记录 / 浏览记录 工具栏按钮 | **0 个**、筛选项 **0 个** | ❌ 企业级不可接受 |

---

## 二、问题诊断（5 类结构性问题）

### 问题 1｜**数据可见性断裂**：4 个列表页没有分页，靠魔法数字截断

- **证据**：`views.py` 中 `checkins` → `records[:200]`、`member-open-records` → `records[:300]`、`notifications` → `[:50]`、`agent` → `conversations[:20]`；`checkin_list.html` / `member_open_record_list.html` 是手写 `<table>`，不走 NetBox 的 `ObjectListView`，因此**没有分页器、没有页大小、没有列配置**。
- **危害**：第 201 条打卡记录对用户**永久不存在且毫无提示**；浏览记录实测一次渲染 104 行、6.1 屏；审计/追溯场景直接失效（这是企业级审计的硬要求）。
- **根因**：这 4 个页面当初为了自定义表格样式绕开了框架的统一列表视图。

### 问题 2｜**首屏信息架构失焦**：仪表板把 14 张卡片一次性铺开

- **证据**：桌面 14 张卡片全在 902px 内（6 列 171px 迷你卡 + 4 张 545px 大卡）；移动端 10 张 KPI 卡纵向堆叠，**主内容起点被推到 y=1204px（1.4 屏以下）**。
- **危害**：桌面端信息密度过高（21px 高的小字卡看不清趋势）；移动端要滑两屏才看到"待审批/逾期"这类**需要行动**的信息——仪表板最重要的事被埋了。
- **根因**：卡片数量按"数据项"而非"决策优先级"排布，缺少 KPI 分层与移动端布局策略。

### 问题 3｜**详情页碎片化**：4–12 张卡片、6–10 个同级标题

- **证据**：成员详情 12 卡片 / 10 标题 / 2.1 屏（移动 3.2 屏）；任务详情 6 卡片 / 1.5 屏；打卡详情 2 卡片但单卡 1039px。
- **危害**：同一实体的信息被切成十几块，用户需要"扫读 + 拼接"；移动端首屏几乎全是标题与卡片边框；维护上每加一个字段就多一张卡。
- **根因**：缺少"详情页信息架构"（概要 / 关联 / 时间线 / 文件）与两栏布局规范。

### 问题 4｜**列表页可操作性不足**：宽表 + 零筛选 + 无行内操作

- **证据**：硬件表头 10 个（含勾选与操作列）、工具 9 个、借出 8 个；打卡记录与浏览记录 **0 筛选器、0 工具按钮**；分页/筛选只在做对了的 5 个页面存在。
- **危害**：数据一多就只能"翻页肉眼找"；无法按时间段、成员、状态过滤；无导出；窄屏全靠横向滚动。
- **根因**：手写列表页没有接入 NetBox 的 `FilterSet + Table + 表格配置` 体系，缺少统一的列表页规范。

### 问题 5｜**设计系统只有视觉层，没有组件层**

- **证据**：318 个 `--tp-*` 令牌（色/主题）说明视觉语言已统一；但 **449 个 `!important`**、8 个模板内联 `<script>`、1 个模板内联 `<style>` 说明缺少组件层与行为层规范；`lab_manager.css` 与 `terminal_pixel.css` 互相覆盖（如 `.calendar-day-checkin` 两处 `background` + `!important` 靠加载顺序决胜）。
- **危害**：新增页面靠"复制粘贴 + 试错"；改一处样式可能影响别处；无法沉淀设计资产，也无法做设计走查。
- **根因**：样式按"页面补丁"演进，未抽象出 `lm-*` 组件类与行为模块。

---

## 三、优化方案

> 每条按 **问题 → 方案 → 验收指标** 给出，验收指标都可用脚本复测（沿用 `reports/qa/ui_audit.js`）。

### 方案 A｜统一列表页规范（解决 P1 / P4）

1. **把所有列表页收敛到一套模式**：`ObjectListView + Table + FilterSet + FilterForm`，复用 NetBox 分页器（`?page=&per_page=`，页大小 25/50/100/250/500）。
   - 迁移对象：打卡记录、浏览记录、通知列表、成员列表、智能体会话。
2. **禁止静默截断**：任何"只显示最近 N 条"必须显式呈现，例如
   `显示最近 200 条 / 共 1,842 条 · [查看全部 →]`。
3. **列的三档视图**：`精简(4–5 列) / 标准 / 完整`，走 NetBox 的表格配置持久化到用户偏好；首列固定 + 粘性表头。
4. **补全列表页能力矩阵**：搜索框、时间范围、成员/状态筛选、导出 CSV、批量操作、空状态引导。
5. **移动端卡片化**：<768px 时宽表自动切成"卡片 + 关键字段 + 操作"，而不是横向滚动 10 列。

**验收**：任意列表页 `?page=2` 可访问；页脚显示"共 N 条 · 第 x/y 页"；每个列表页至少有 1 个搜索 + 1 个筛选 + 导出；窄屏无横向滚动。工作量：**1.5–2 天**（5 个页面，多数可复制现有 `object_list.html` 模式）。

### 方案 B｜仪表板重构为"决策优先"的信息层级（解决 P2）

推荐结构（桌面）：

```
┌─ KPI 行（4 张，高 ≤96px，含迷你趋势线）───────────────────┐
│ 硬件在用 3/5 ▲  任务进行中 2  ⏰今日打卡 3  ⚠️逾期 1      │
├─ 需要我处理（左 8 列，主区）───────┬─ 库存水位（右 4 列）─┤
│ 待审批硬件(2) / 逾期未还(1) /     │ 水位条 + 阈值色      │
│ 低库存(1) —— 每项带一键操作按钮    │ 采购建议入口         │
├─ 趋势（左 8 列，折线/柱状）────────┼─ 最近活动（右 4 列）─┤
│ 近 30 天任务完成率 / 借出量        │ 事件流（时间轴）     │
└────────────────────────────────────┴──────────────────────┘
```

- 移动端：KPI 改**横向 scroll-snap 条**（高 ≤96px），首屏必须露出"需要我处理"的前 2 条；把 14 张卡压缩为 4 KPI + 3 待办 + 1 水位 + 1 事件流。
- 次要指标（如各类占比）收进"更多指标"折叠区或 `/analytics/` 子页。

**验收**：桌面无滚动即可见 KPI + 待办；移动端首屏（844px）内出现待办 ≥2 条；仪表板总屏数 ≤2。工作量：**1 天**。

### 方案 C｜详情页两栏化 + 语义分区（解决 P3）

- ≥1200px：左 8 列（概要 + 时间线），右 4 列（元数据 + 操作 + 关联快捷入口）；<1200px 单栏堆叠。
- 语义分区数从 6–12 压到 **≤4**：`概要` / `关联对象(Tab: 任务·借出·打卡)` / `活动时间线` / `附件`。
- 卡片合并：同一语义的字段用 definition list（`<dl>` 两列）而不是一卡一项；卡片标题只保留 h2。
- 长内容（浏览记录、GPS 明细）改为 `Accordion` 或独立 Tab，默认折叠。

**验收**：成员详情 ≤2 屏且分区 ≤4；移动端 ≤3 屏；同级标题 ≤4 个。工作量：**1.5 天**（成员详情、任务详情、硬件详情、打卡详情）。

### 方案 D｜数据可视化升级（把"数字"变成"可判断的信息"）

| 场景 | 现状 | 建议 |
|---|---|---|
| 库存 | 纯数字 `quantity` 列 | **水位条**：`可用/总量`，低于 `minimum_stock` 变橙、=0 变红 |
| 任务进度 | 状态文字 | **看板视图**（拖拽改状态，超期卡红色描边）+ **甘特时间轴**（按执行人分组） |
| 考勤 | 表格 + 数字 | **年历热力图**（GitHub 贡献图风格），点某天钻取当日打卡 |
| 借出 | 列表 | **借用周期条**（借出→预计→实际归还三段时间轴） |
| 趋势 | 无 | 近 30 天折线：任务完成率、借出量、打卡人数 |
| 智能体 | 纯文本回复 | 回复中的表格/统计**内嵌图表卡片**（可导出） |

技术选型：优先 **轻量 SVG/CSS 图表**（<10KB，无依赖、与像素主题一致），复杂图表再引入单一图表库（如 ECharts 按需加载，`lm-charts.js` 统一封装）。

**验收**：库存页可用颜色一眼识别缺货；任务页可在看板内完成状态流转；考勤页 1 屏看完一年。工作量：**3–4 天**（看板 1.5 天、热力图 0.5 天、水位条 0.5 天、趋势图 1 天）。

### 方案 E｜设计系统分层（解决 P5）

**三层令牌**（在现有 318 个 `--tp-*` 之上收敛）：

| 层 | 示例 | 说明 |
|---|---|---|
| primitive | `--c-cyan-500:#00e5ff` `--space-2:8px` `--font-13:13px` | 色阶、间距、字号 |
| semantic | `--surface-1/2/3` `--border-subtle` `--text-primary/muted` `--accent` `--danger` `--warning` `--ok` | 按用途命名，主题切换只改这一层 |
| component | `--card-radius` `--table-row-h` `--btn-h` | 组件级 |

**组件类规范**（替代 `!important` 对抗）：`.lm-card` `.lm-card__header` `.lm-table` `.lm-toolbar` `.lm-kpi` `.lm-empty` `.lm-timeline` `.lm-badge`，全部基于令牌、零 `!important`。

**行为层**：8 处内联 `<script>` 收敛为 `lm-ui.js`（交互：抽屉/复制/确认/快捷键）与 `lm-charts.js`（图表），模板只留 `data-*`。

**验收**：`!important` 从 449 降到 ≤50；模板无内联 `<script>`（仅数据 JSON）；新增页面只组合 `lm-*` 类。工作量：**2–3 天**（可增量，不必一次重写）。

---

## 四、创意设计提案（企业级亮点）

1. **实验室指挥舱 / 值班大屏（Mission Control）**
   单屏 4 分区：KPI 条 · 待办队列 · 库存水位 · 实时事件流。提供"投屏模式"：暗色高对比、字号放大 1.5 倍、30 秒自动刷新、Escape 退出。契合电赛/实验室现场值守场景，也是最有"企业级观感"的一屏。

2. **AI 副驾驶常驻侧栏（Context-aware Copilot）**
   任意页面右上角唤起，自动携带当前上下文（"你正在看硬件 #3 Test MCU Board"），提供上下文相关动作：*生成借用单 / 写采购建议 / 总结该成员近期活动 / 找出相似任务*。把现有智能体从"独立页面"升级为"全站副驾驶"——这是最能体现差异化的设计。

3. **命令面板（Ctrl / ⌘ + K）**
   聚合"跳转 + 操作 + 搜索"：输入"借 3 号开发板给胡涵"直接弹出预填的借出表单；输入"待审批"直接跳到筛选后的硬件列表。落地成本低（一个模态 + 一个 JSON 索引），体验跃升明显。

4. **库存水位 + 阈值色阶**
   把 `quantity / minimum_stock` 渲染成水位条（绿→橙→红），鼠标悬停显示"在借 N · 可用 M · 阈值 K"。低库存项在列表顶部用一条"采购待办"横幅汇总。

5. **考勤热力图 + 异常提示**
   年历热力图（深浅 = 当日打卡次数），叠加"连续未打卡 ≥3 天"的标记；点击某天右侧抽屉展示当日打卡详情与照片缩略图。

6. **像素终端彩蛋：`>` 命令行条**
   在主题里已经有的"终端"气质上，加一个可选的底部命令条（默认收起），支持 `?` 帮助、`/hw 开发板`、`/task done 12` 等极简操作，强化品牌一致性（对实验室用户友好，且不影响普通用户）。

---

## 五、实施路线图

### P0（1–2 天，投入产出比最高，建议立刻做）

| 项 | 内容 | 验收 |
|---|---|---|
| 1 | 打卡记录、浏览记录、通知、成员、智能体会话接入统一分页（`?page&per_page`） | 每页 `?page=2` 可访问；无硬编码截断 |
| 2 | 打卡记录 / 浏览记录补筛选 + 导出 + 工具按钮 | 工具栏按钮 ≥3、筛选项 ≥3 |
| 3 | 仪表板移动端 KPI 横滑 + 待办上移 | 移动首屏露出待办 ≥2 条 |
| 4 | 成员详情两栏化 + 卡片合并到 ≤4 分区 | 桌面 ≤2 屏 |
| 5 | 空状态与骨架屏（列表/详情/AI 回复） | 无数据页有引导按钮；加载有骨架 |

### P1（3–5 天）

| 项 | 内容 |
|---|---|
| 6 | 列表页"三档列视图" + 粘性表头 + 首列固定 + 移动端卡片化 |
| 7 | 设计令牌三层化 + `.lm-*` 组件类 + `!important` 清理至 ≤50 |
| 8 | 库存水位条 + 低库存横幅；任务看板视图（拖拽改状态） |
| 9 | 详情页时间线与关联对象 Tab（任务/借出/打卡统一） |
| 10 | 8 处内联脚本收敛到 `lm-ui.js` / `lm-charts.js` |

### P2（1–2 周，亮点与体系）

| 项 | 内容 |
|---|---|
| 11 | 指挥舱 / 值班投屏模式 |
| 12 | 考勤热力图 + 借出甘特 + 30 天趋势图 |
| 13 | 命令面板（⌘K） |
| 14 | AI 副驾驶侧栏（上下文感知） |
| 15 | 组件文档页 `/plugins/lab-manager/design-system/`（令牌 + 组件 + 用法示例），作为团队设计资产的单一来源 |

---

## 六、设计令牌草案（可直接落地）

```css
:root {
  /* primitive */
  --space-1:4px; --space-2:8px; --space-3:12px; --space-4:16px; --space-6:24px; --space-8:32px;
  --font-12:12px; --font-13:13px; --font-14:14px; --font-16:16px; --font-20:20px; --font-24:24px;
  --radius-sm:3px; --radius-md:6px; --radius-lg:10px;
  --dur-fast:120ms; --dur-base:200ms;

  /* semantic（主题切换只改这一层） */
  --surface-1:#0b0e14; --surface-2:#111726; --surface-3:#161d2e;
  --border-subtle:rgba(0,229,255,.14); --border-strong:rgba(0,229,255,.32);
  --text-primary:#e6f1ff; --text-muted:#8ea0b8; --text-inverse:#0b0e14;
  --accent:#00e5ff; --accent-soft:rgba(0,229,255,.12);
  --ok:#00ff41; --warning:#ffaa33; --danger:#ff5566; --info:#4aa8ff;

  /* component */
  --card-radius:var(--radius-md); --card-pad:var(--space-4);
  --table-row-h:40px; --table-head-h:36px;
  --btn-h:32px; --btn-h-lg:40px;
  --kpi-h:96px; --toolbar-h:56px;
}
```

**组件清单（P1 落地）**：`lm-card` `lm-kpi` `lm-toolbar` `lm-table`（含 sticky/fixed-col）`lm-badge`（状态语义色）`lm-empty` `lm-skeleton` `lm-timeline` `lm-waterline`（水位条）`lm-heatmap` `lm-drawer` `lm-command-palette`。

---

## 七、附：复测方法

```bash
cd reports/qa
node ui_audit.js     # 全站 19 页 × 桌面/移动：滚动屏数、卡片数、主内容起点、分页
node ui_audit2.js    # 分页能力（?per_page=1）、仪表板构成、列数与筛选项
```

优化后重跑这两个脚本，对比 `screens`（滚动屏数）、`分页`、`筛选项`、`!important` 数量四项指标即可量化验收。

---

## 八、实施结果（2026-09 已落地部分）

### 8.1 量化对比（`node reports/qa/ui_audit.js`）

| 页面 | 桌面 屏数（前→后） | 移动 屏数（前→后） | 移动主内容起点 | 关键变化 |
|---|---|---|---|---|
| 仪表板 | 1.0 → 1.0 | 2.0 → **1.6** | 1204px → **915px** | KPI 行移动端横滑（scroll-snap），待办区上移 |
| 浏览记录 | **5.7 → 1.9** | **24.7 → 4.5** | 608px → 869px | 接入分页（默认 25/页）+ 筛选 + 导出 + 移动卡片化 |
| 打卡记录 | 1.0 → 1.0 | 1.0 → 1.4 | 230px → 638px | 筛选栏 + 分页 + 移动卡片化 |
| 任务列表 | 1.2 → 1.1 | 1.9 → **1.3** | — | 粘性表头 + 密度切换 |
| 成员详情 | 2.1 → 2.1 | 3.2 → **3.0** | 861px → 725px | 统计行移动端横滑 |
| 工具管理 | 1.0 → 1.0 | 1.5 → **1.3** | — | 密度切换 + 粘性表头 |

**汇总**：修复前「滚动超过 3 屏」的页面 1 个、无分页页面 4 个；**修复后桌面端 0 个超 3 屏、0 个无分页**。

### 8.2 已交付能力

**P0（全部完成）**
1. 5 个列表页统一分页（`?page=&per_page=`，25/50/100/200，页脚显示"共 N 条 · 第 x/y 页"）：打卡记录、浏览记录、通知中心、成员列表、智能体会话。
2. 打卡记录 / 浏览记录：成员+关键词+日期范围筛选、CSV 导出（带 BOM）、空状态引导。
3. 仪表板：移动端 KPI 横滑、新增「需要我处理」语义分区。
4. 成员详情：统计行移动端横滑（进一步两栏化列入待办）。
5. 空状态组件（`inc/empty_state.html`）+ 骨架屏组件（`.lm-skeleton`）。

**P1（部分完成）**
6. 表格密度切换（紧凑/舒适，localStorage 持久化）+ 框架表格粘性表头 + 首列固定。
7. 组件层 `lm-components.css`：三层令牌（primitive/semantic/component）+ 20 个 `lm-*` 组件类，**新文件零 `!important`**。
8. 库存水位条（硬件列表数量列渲染为水位条，含阈值配色）+ 低库存横幅；硬件列表默认列从 8 列精简到 5 列。
9. 任务看板（三列拖拽流转，逾期红色描边，`/tasks/board/`）。
10. 详情时间线组件（`.lm-timeline`）已用于指挥舱事件流。

**P2（全部完成）**
11. 指挥舱 `/mission-control/`：KPI 条 + 待办队列（审批/逾期/低库存，带一键操作）+ 库存水位 + 实时事件流 + **26 周考勤热力图** + 30 天三序列趋势（服务端 SVG，无第三方图表库）+ **投屏模式**（隐藏侧栏、字号放大、60 秒自动刷新、全屏）。
12. 设计系统页 `/design-system/`：令牌表 + 9 类组件示例 + 使用约定。
13. 命令面板 **⌘K**：`/api/command-index/` 动态索引（导航 22 项 + 操作 + 硬件/任务对象），支持上下键选择与回车跳转。
14. AI 副驾驶：全站浮动入口，携带页面上下文调用现有智能体，支持多轮会话延续。
15. 全站 UI 由 `base/layout.html` 统一注入（组件层 CSS + 全局 UI 片段 + `lm-ui.js`），交互行为集中在 1 个文件、`data-*` 驱动。

### 8.3 仍未完成（建议下一轮）

| 项 | 现状 | 建议 |
|---|---|---|
| 成员/任务详情语义分区与关联 Tab | 成员详情仍 12 卡片 / 10 标题 / 2.1 屏 | 按"概要 / 关联对象 Tab / 时间线 / 附件"四分区重构（约 1.5 天） |
| 内联脚本收敛 | 仍有 7 个模板内联 `<script>`（智能体控制台最大） | 逐个迁移到 `lm-ui.js`，智能体控制台单独排期（回归风险高） |
| 借出甘特 / 周期条 | 未实现 | 复用 `.lm-chart` 的 SVG 渲染 |
| 列视图三档（精简/标准/完整） | 仅做了密度切换 + 硬件默认列精简 | 基于 NetBox 表格配置 + 用户偏好实现每表三档 |
| `!important` 清理 | 既有主题层仍有 449 处 | 随页面重构逐步替换为 `lm-*` 组件类，目标 ≤50 |
| 浏览记录移动端 | 4.5 屏（25 行卡片） | 移动端默认页大小降到 10，或改为虚拟滚动 |

---

## 九、显示缺陷复盘（截图审查发现并已修复）

在浏览器里逐页看图后发现两处**只有肉眼/截图才能发现**的显示问题，已修复：

### 9.1 浅色主题下卡片标题不可见（严重）

- **现象**：设计系统等新页面在浅色主题下，卡片标题（`.lm-card__header`）**几乎看不到**（白字压米色底）；骨架屏渲染成黑色实心条。
- **根因**：组件层语义令牌引用了主题里**不存在**的变量 ——
  `--lm-text-primary: var(--tp-card-text, #e6f1ff)`，但主题实际定义的是
  `--tp-card-header-text` / `--tp-card-body-text`，`--tp-card-text` 从未定义，
  于是永远回退到我写死的暗色 `#e6f1ff`；`--tp-card-hover-bg` 同理不存在 → 骨架屏回退成暗色。
- **修复**：语义层改引用真实存在的主题变量，最后一级回退改为中性值：
  `--lm-surface-2: var(--tp-table-hover-bg, rgba(127,127,127,.08))`、
  `--lm-text-primary: var(--tp-card-header-text, var(--tp-card-body-text, var(--bs-body-color, #2b2b2b)))` 等；
  状态色按主题分别定义（浅色用深一档，暗色用高亮）。
- **验证**（`node reports/qa/ds_contrast.js`，WCAG AA 需 ≥4.5:1）：

  | 元素 | 修复前 | 修复后（浅色） | 修复后（暗色） |
  |---|---|---|---|
  | 卡片标题 | **1.08:1（不可见）** | **6.0:1 ✅** | 11.62:1 ✅ |
  | KPI 标签 | 5.91:1 | 5.91:1 ✅ | 6.11:1 ✅ |
  | KPI 数值 | 11.87:1 | 11.87:1 ✅ | 13.95:1 ✅ |
  | 空状态描述 | 5.91:1 | 5.91:1 ✅ | 6.11:1 ✅ |

  全部文本 ≥5.4:1，无 10px 以下文字。

### 9.2 趋势图在无数据时是一片空白

- **现象**：指挥舱「近 30 天趋势」在三条序列全为 0 时，折线贴在容器边缘，看起来像渲染失败。
- **修复**：新增 `trend.empty` 判定 → 显示空状态引导；有数据时补基线与峰值参考线、起止日期与峰值刻度。

### 9.3 顺带修掉的两个隐藏问题

1. **命令面板/密度按钮的宿主块是死块**：`object_list.html` 里的 `{% block controls %}` 在主题布局（只渲染 `page`/`content`）中**从不渲染**，导致密度按钮（以及原模板里的插件列表按钮）一直没出现。已把工具条移入 `{% block content %}`。
2. **空列表时分页器不显示**：Django 的 `Page` 有 `__len__`，**0 条时 `{% if page_obj %}` 判为假**，分页器整块被跳过（通知中心 0 条时看不到"共 0 条/每页"）。判据改为 `{% if page_obj.paginator %}`。

### 9.4 新增回归工具

| 脚本 | 用途 |
|---|---|
| `reports/qa/ds_contrast.js` | 亮/暗两套主题下解析令牌值 + 计算文本对比度（WCAG）+ 检测 <10px 文字 |
| `reports/qa/health_check.py` | 17 个主要页面：状态码 + 侧栏存在性 + 关键组件标记 |
| `reports/qa/verify_ui_interaction.js` | 交互层：⌘K 面板（打开/索引/过滤/Esc）、密度切换持久化、副驾驶、投屏、看板拖拽、JS 报错 |
| `reports/qa/ds_diag.js` | 单页布局体检：CSS 加载、溢出、零高度、区块顺序与尺寸 |

---

## 十、使用手感优化（交互流畅度）

用户反馈"切换界面、使用功能时手感很怪"。量化后定位到三个根因并修复：

### 10.1 根因与修复

| 问题 | 测量证据 | 修复 |
|---|---|---|
| **全屏无限动画一直在跑** | 扫描线 1.5s 无限循环 + 粒子 22s/28s 无限，均为 `position:fixed` 全屏层，页面**永不停止重绘** | 默认静音（保留静态质感），可在工具栏/命令面板开启「特效」找回动效；同时尊重 `prefers-reduced-motion` |
| **每次翻页都要等入场动画** | `animations.js` 桌面端 `DURATION=800ms`，卡片再按 `index*60ms` 错峰 → 最晚的卡片要 1s 后才稳定 | 时长 800→**260ms**、错峰 60→18ms，并**只在本次会话首次加载播放**（`sessionStorage`），后续翻页内容立即呈现 |
| **大面毛玻璃 + 侧栏/顶栏模糊** | 4 处 `backdrop-filter`（含侧栏与顶栏）在滚动时持续合成 | 默认对侧栏/顶栏关闭毛玻璃，仅保留小面积（倒计时卡片、命令面板） |

另加两项正向优化：
- **悬停预取**：鼠标移到链接即 `prefetch` 目标页，点击后命中缓存（每页最多预取一次）。
- **页面淡入 140ms（仅 opacity）**：切换时不出现"白闪"，也不产生布局抖动。

### 10.2 量化前后对比（`node reports/qa/verify_feel.js`）

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 全屏无限动画 | 3 个（永不停止） | **0 个** |
| 首次加载内容完全可见 | ~800ms + 卡片错峰（>1s） | **785ms** |
| 第二次导航内容完全可见 | 每次都等动画 | **171ms** |
| 第三次导航内容完全可见 | 同上 | **102ms** |
| 点击侧栏跳转耗时 | — | 418 / 592 / 651ms |
| 滚动期间主线程长任务 | 有（全屏动画重绘） | **0ms** |
| 可见毛玻璃元素 | 4 处 | 1 处（小面积） |
| 特效开关 | 无 | 开启后动画恢复（4 个） |

回归：31/31 测试通过、全站 0 个 500、17 页体检通过、交互层 12/12 通过。

### 10.3 仍可继续优化（部署侧为主）

1. **首屏 785ms 主要来自开发服务器**：`runserver` 单进程 + 未压缩 + 每次渲染模板。生产用 gunicorn/uvicorn + Whitenoise/nginx（gzip + 强缓存）后，CSS 体积（本地约 1MB 未压缩）与 TTFB 都会显著下降。
2. **成员列表页点击 651ms**：该页有 19 次聚合查询，可合并为 2–3 次（`values().annotate()` 一次算完任务/借出/打卡/项目）。
3. **表单提交后整页刷新**：可对"标记已读/启停工具/批量操作"改 HTMX 局部刷新，避免整页重绘（NetBox 已内置 HTMX）。
4. **导航仍是整页跳转**：如需接近 SPA 的手感，可在侧栏启用 `hx-boost`（渐进增强，失败自动回退整页跳转）。

---

## 十一、侧栏"每次切换都刷新"的修复（局部导航）

### 11.1 排查结论

- 实测侧栏激活项在加载后的计算样式只有 **1 个状态** → 侧栏**没有重放动画**；
- 所以你看到的"菜单刷新"本质是**整页重载导致整个文档重绘**（菜单短暂消失再出现）。
- 曾尝试跨文档 View Transitions（`@view-transition` + 侧栏 `view-transition-name`），规则已就位但本机 Chrome 152 下 `pagereveal` 未触发，不可依赖（CSS 保留，支持的环境自动受益）。

### 11.2 采用方案：HTMX 局部导航（只换内容区）

- 在侧栏 `<aside>` 上加 `hx-boost="true" hx-target="#page-content" hx-select="#page-content" hx-swap="innerHTML show:window:top" hx-push-url="true"`；
- 点击菜单时**只替换 `#page-content`**，侧栏 DOM 完全保留；
- 换页后自动：更新 `<title>`、重新执行内容区内联脚本、重新绑定交互（密度/特效/行点击/看板），并做了幂等保护（`data-lm-bound`）与 MutationObserver 兜底（不依赖 htmx 事件名）；
- **白名单/黑名单**：含内联脚本或文件上传的页面（智能体控制台、拍照打卡）在捕获阶段拦截，回退整页跳转，避免样式/脚本缺失；
- 静音 `htmx:oobErrorNoTarget` 诊断（只换内容区时 NetBox 的 OOB 片段找不到目标，纯提示、不影响功能）。

### 11.3 量化结果（`node reports/qa/verify_boost.js`）

| 指标 | 整页跳转 | 局部导航 |
|---|---|---|
| 侧栏 DOM 是否重建 | 是（每次重建） | **否（同一节点，`sameNode=true`）** |
| 点击菜单 → 内容就绪 | 418–651ms | **305–312ms** |
| 标签页标题 | 自动 | 自动（从响应 `<title>` 更新） |
| 黑名单页面 | — | 正确回退整页跳转 |

回归：31/31 测试通过 · 17 页体检通过 · 局部导航 9/9 · 交互层 12/12 · 全站 0 个 500。

### 11.4 局部导航的副作用修复：侧栏高亮（回归 bug）

**现象**：改局部导航后，侧栏"当前项"高亮不再变化，一直停在「我的任务」。

**原因**：高亮原本是**服务器渲染**的（`active` 类写在 `.dropdown-item` 外层 div 上）。
局部导航只替换 `#page-content`，侧栏 HTML 从不重新渲染，于是高亮永远停在进入局部导航时的那一项。

**修复（两层）**：

1. **`lm-ui.js` 新增 `syncNavActive()`**：按当前 URL 重算"最长前缀匹配"的侧栏链接，
   把 `active` 加回它所在的行（与服务器渲染位置一致），并给精确命中的链接写 `aria-current="page"`；
   在 `afterSwap / afterSettle / afterOnLoad / pushedIntoHistory / popstate` 以及 80ms 兜底定时器上触发。
   同时**排除行内小图标按钮**（如「录入硬件」`/hardware/add/`），否则在新增页会高亮到加号按钮上。
2. **菜单结构修正**：`打卡记录` 原本是挂在「拍照定位打卡」行内的 `PluginMenuButton`（一个小图标），
   导致 `/checkins/` 的高亮只能落在「拍照定位打卡」整行上（同一 `.dropdown-item` 被两行共用），
   看起来像"点错了"。已提升为独立菜单项 `打卡记录`，高亮与点击目标一一对应。
   → 实测 `/checkins` 高亮 = 打卡记录；`/hardware/add/` 高亮 = 硬件列表。

**顺带修掉的视觉问题**：主题只强调顶级 `.nav-link.active`（分组标题），子项 `.dropdown-item.active`
只有 6% 黑底，暗色主题下几乎不可见。`lm-components.css` 补齐：强调色派生底 + 内嵌 3px 左色条
（用 `box-shadow` 而非 `border`，避免文字位移）+ 精确项前缀标记 `›`/`▸`（`lm-effects` 关闭时退化为字符）。
注意必须提高选择器特异性：NetBox 暗色规则
`html[data-bs-theme="dark"] .navbar-vertical.navbar-expand-lg .dropdown-item.active` 的类计数高于普通写法，
普通 `!important` 会被它压过（实测背景被打回 `rgba(255,255,255,.06)`）。

验证：`node reports/qa/verify_nav_active.js` **14/14 通过**（含 `aria-current` 精确性、新增页归属、
无 JS 报错）；截图 `reports/qa/shots/nav_checkins_{light,dark}.png`。
